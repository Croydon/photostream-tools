<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hochschuledarmstadt.photostream_tools.examples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/PhotoStreamToolsTheme">
        <activity android:name=".photo.PhotoActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".comment.CommentActivity" android:exported="true"></activity>

        <meta-data android:name="PHOTOSTREAM_URL" android:value="http://5.45.97.155:8082" />

    </application>

</manifest>
